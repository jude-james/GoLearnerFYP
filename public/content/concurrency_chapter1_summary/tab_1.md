## Goroutines

A goroutine is a lightweight independently executing function. You launch one with the `go` keyword in front of a function call. Unlike threads, you can have thousands of them running at the same time.