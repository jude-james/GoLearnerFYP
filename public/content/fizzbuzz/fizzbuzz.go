package main

import "fmt"

func main() {
	fizzCount := 0

	for i := 1; i <= 30; i++ {
		switch {
		// TODO: add cases for FizzBuzz, Fizz, Buzz and default
		// Then increment fizzCount when "Fizz" or "FizzBuzz" is printed
		default:
		}
	}

	fmt.Println("Fizz count:", fizzCount)
}
