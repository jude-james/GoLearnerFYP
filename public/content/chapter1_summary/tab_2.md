## fmt is your go-to output package

The `fmt` package handles all formatted printing. The three functions you'll use most:

- `fmt.Println` — prints with a newline
- `fmt.Printf` — prints using format verbs like `%s`, `%d`, `%f`
- `fmt.Sprintf` — builds a formatted string without printing it