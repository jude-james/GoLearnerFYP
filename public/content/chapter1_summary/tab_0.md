## Every Go program starts with a package

All Go code belongs to a package. Executable programs must declare `package main`, and execution begins in the `func main()` function. Without these two things, your program won't run.